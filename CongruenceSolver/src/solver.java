import java.util.ArrayList;

public class solver {
    public static void solve(ArrayList<Integer> vals, ArrayList<Integer> mods) {
        if(vals != null && mods != null && vals.size() == mods.size()) {
            
        }
    }
}
